/* formatterSettingsOverride */
/*  { "AblFormatter.findFormatting": true, "AblFormatter.findFormattingWhereLocation": "New"}*/

FIND LAST Simile WHERE Simile.Comparison BEGINS "like" AND Simile.Strength > 7 AND Simile.IsEffective = TRUE NO-LOCK NO-ERROR.
